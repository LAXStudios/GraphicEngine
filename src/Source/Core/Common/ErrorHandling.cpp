#include "../../../Headers/Core/Common/ErrorHandling.h"
/*
voidid GLClearError() {
  while (glGetError())
    ;
}

bool GLPrintErrors(std::string functionName, std::string file, int line) {
  bool hasErrors = false;
  while (GLenum err = glGetError()) {
    hasErrors = true;
    std::cout << "OpenGL ERROR: " << err << " in " << line << " in " << file
              << "\n";
  }
  return hasErrors;
}
*/
